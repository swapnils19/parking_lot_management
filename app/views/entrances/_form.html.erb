<%= form_with(model: [entrance.parking_lot, entrance]) do |form| %>
  <% if entrance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(entrance.errors.count, "error") %> prohibited this entrance from being saved:</h2>

      <ul>
        <% entrance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
