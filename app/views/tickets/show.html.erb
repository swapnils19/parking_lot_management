<p style="color: green"><%= notice %></p>

<%= render @ticket %>

<div>
  <%= link_to "Back to tickets", parking_lot_tickets_path(@parking_lot) %>
  <%= button_to "Destroy this ticket", parking_lot_ticket_path(@parking_lot, @ticket), method: :delete %>
</div>
