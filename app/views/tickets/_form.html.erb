<%= form_with(model: [ticket.parking_lot, ticket]) do |form| %>
  <% if ticket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
        <% ticket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :car_id, style: "display: block" %>
    <%= form.collection_select :car_id, Car.select(:id), :id, :id %>
  </div>

  <div>
    <%= form.label :spot_id, style: "display: block" %>
    <%= form.collection_select :spot_id, @parking_lot.spots.select(:id), :id, :id %>
  </div>

  <br/>

  <div>
    <%= form.submit %>
  </div>
<% end %>
